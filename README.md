# Engram
High-performance vector search engine built from scratch in C++. Engram is optimized for low-latency RAG applications, featuring contiguous memory layout, custom binary persistence, and fast similarity search without external dependencies.
